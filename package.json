{
  "name": "quadrum-cz",
  "version": "1.0.0",
  "description": "Quadrum.cz – finanční a realitní poradenství. Statický web + Vercel serverless API.",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "migrate:r2": "node scripts/migrate-to-r2.js",
    "update:paths": "node scripts/update-media-paths.js",
    "fix:urls": "node scripts/fix-duplicate-urls.js",
    "dev": "vercel dev",
    "deploy:preview": "vercel",
    "deploy:prod": "vercel --prod"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.490.0",
    "dotenv": "^16.3.1",
    "mime-types": "^2.1.35"
  }
}
